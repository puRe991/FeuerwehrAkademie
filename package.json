{
  "name": "feuerwehr-akademie",
  "version": "1.0.0",
  "private": true,
  "description": "Feuerwehr Online Akademie – mobile App (Android via Capacitor). Verpackt die bestehende PWA in eine native App.",
  "type": "module",
  "scripts": {
    "build:web": "node scripts/build-web.mjs",
    "sync": "npm run build:web && cap sync android",
    "assets": "capacitor-assets generate --android",
    "open:android": "cap open android",
    "apk:debug": "npm run sync && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/cli": "^6.2.1",
    "@capacitor/core": "^6.2.1"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5"
  }
}
